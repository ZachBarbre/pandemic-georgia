require.router = shuffle
const draw = ${infectionlevel},
discard = discard,
infectDeck = infectdeck;

function disco (){
    shift.infectDeck()
    let a = 0;
     shuffle (discard)
     add discard to infectDeck
    while a < draw{
     pop.infectdeck()
     a = a+1}   
}

